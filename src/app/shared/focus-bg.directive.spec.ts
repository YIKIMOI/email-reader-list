import { FocusBgDirective } from './focus-bg.directive';

describe('FocusBgDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusBgDirective();
    expect(directive).toBeTruthy();
  });
});
